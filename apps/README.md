# Apps Directory

This directory is the draft v2 home for manifest-driven app definitions.

Each app should live at:

```text
apps/<app>/
├── manifest.yaml
├── files/
└── hooks/
```

Use `scripts/add-app <name>` to scaffold a new app.
